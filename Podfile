# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'slack-clone-ios-app' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire', '~> 5.2'

  # Pods for slack-clone-ios-app

  target 'slack-clone-ios-appTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'slack-clone-ios-appUITests' do
    # Pods for testing
  end

end
